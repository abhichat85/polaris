import { SettingsView } from "@/features/settings/components/settings-view";

export const metadata = {
  title: "Settings · Polaris",
};

const SettingsPage = () => {
  return <SettingsView />;
};

export default SettingsPage;
