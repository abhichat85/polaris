# Polaris — Final Completion Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Take Polaris from current state (~85% Constitutional compliance) to **100% — every pending item closed end-to-end**, every TODO resolved, every test gap filled, every UI affordance shipped, every audit gap addressed.

**Architecture principle:** Phases are ordered by dependency, not by audit number. A later phase can never compile against an unshipped earlier phase. Each task is a self-contained subagent session of ~30–90 minutes.

**Tech stack:** Next.js 16 + Convex + Clerk + Inngest + E2B + Stripe + Sentry + Vitest + Playwright + Upstash.

---

## Snapshot of pending items (sweep results, 2026-04-27)

This list was generated by grepping the codebase for `TODO`, `FIXME`, `stub`, `Mock`, `test.fixme`, by reading `docs/CONSTITUTION.md` decision log, and by tracing the imports of `"ai"` + `@ai-sdk/*`. **Anything <100% counts as pending.**

| # | Item | Source |
|---|---|---|
| 1 | `agent-loop.ts:30,84` hardcodes `MockSandboxProvider` instead of `getSandboxProvider()` singleton | grep |
| 2 | Per-project E2B sandbox lifecycle not wired (sandboxes table exists but unused) | docs + grep |
| 3 | `run_command` tool not registered in `code-agent.ts` (system prompt removed it) | grep + commit history |
| 4 | `messages.toolCalls[].stream[]` field missing on validator | schema review |
| 5 | `<ToolOutputStream />` UI component missing | grep |
| 6 | `appendToolStream` Convex mutation missing | code review |
| 7 | 6 UI files type-only-import from `"ai"` (image, message, context, prompt-input, tool, confirmation) | grep |
| 8 | `package.json` still has `@ai-sdk/anthropic`, `@ai-sdk/google`, `ai` | package.json |
| 9 | CONSTITUTION.md decision log stops at D-017; D-018 (run_command), D-019 (plans), D-020 (workspaces), D-021 (Stripe webhook idempotency), D-022 (assertWithinQuotaInternal pattern) all missing | docs/CONSTITUTION.md |
| 10 | Article §8 still says "7 tools" — should be 8 (edit_file added per D-017) | docs/CONSTITUTION.md |
| 11 | `projects.workspaceId` is optional — should become required after backfill verified | schema |
| 12 | Project queries don't filter by `workspaceId` | convex/projects.ts |
| 13 | `projects.create` doesn't take `workspaceId` arg | convex/projects.ts |
| 14 | Auto-create personal workspace on first sign-in (Clerk webhook) | schema TODO comment |
| 15 | Member invite UI in Settings → Workspace (CLI hint shown) | settings-view.tsx |
| 16 | Email-based member lookup (currently shows raw Clerk userId) | settings-view.tsx |
| 17 | Workspace switcher → actually scope projects (currently read-only) | workspace-switcher.tsx |
| 18 | Plan tier badge in IDE rail | UI gap |
| 19 | Usage meter in Settings → Billing (current vs limit per quota) | UI gap |
| 20 | `/pricing` page has tier cards but no Stripe Checkout integration — `cta.href` points to `/sign-up` | pricing/page.tsx |
| 21 | `/api/billing/checkout` (create Stripe Checkout Session) endpoint missing | grep |
| 22 | `/api/billing/portal` (Stripe Customer Portal redirect) endpoint missing | grep |
| 23 | Stripe webhook unit tests missing | grep |
| 24 | Workspaces unit tests missing (create, invite, role boundaries, last-owner protection) | grep |
| 25 | `assertWithinQuota` handler-level unit test missing (only tier-number lock test exists) | tests dir |
| 26 | 5 E2E specs are `test.fixme` — un-fixme as preconditions land | tests/e2e |
| 27 | Clerk testing-token helper missing for E2E | grep |
| 28 | Consistency model untested — checkpoint resume + sandbox resync | docs §10 |
| 29 | Sentry performance instrumentation (P50/P95 time-to-preview, agent iteration < 3s/8s) | docs §14 |
| 30 | Sentry alert rules not configured | docs §15 |
| 31 | Upstash rate limiter exists but not applied to any API route | grep |
| 32 | Per-tier rate limits (free/pro/team) | docs §17 implied |
| 33 | `/api/messages:70` TODO "Check for processing messages" | grep |
| 34 | `use-files.ts` 4× TODO "Add optimistic mutation" | grep |
| 35 | Praxiom integration `/api/praxiom/import` 501 stub missing | docs §18 |
| 36 | README.md / ARCHITECTURE.md / RUNBOOK.md not updated | filesystem |
| 37 | `quick-edit` + `suggestion` routes don't have rate limiting | grep |
| 38 | Workspace plan field doesn't reconcile with `customers.plan` (drift risk) | schema review |
| 39 | `seedDefaults` numbers in `convex/plans.ts` differ from `/pricing` page numbers (50K tokens vs "100 agent runs" copy mismatch) | cross-ref |
| 40 | Inngest function `agent/run` event not yet replacing `message/sent` (legacy `processMessage` still active per Article XIX migration) | inngest/route.ts |

**Total: 40 distinct pending items.** Plan below addresses every one.

---

## Phase 0 — Constitutional housekeeping (1–2 hrs)

**Why first:** Every code change below references a Decision-Log entry. Amending the Constitution before shipping the code lets each PR commit message cite an authoritative D-NNN.

### Task 0.1 — Add D-018 through D-022 to CONSTITUTION.md

**Files:** `docs/CONSTITUTION.md`

- [ ] Open `docs/CONSTITUTION.md` decision log (after D-017).
- [ ] **D-018:** Sandbox lifecycle is per-project, persisted in `sandboxes` table, reprovisioned on `SandboxDeadError`. Sandbox provider is selected at runtime by `getSandboxProvider()` (env: `SANDBOX_PROVIDER`).
- [ ] **D-019:** `plans` table is the source of truth for tier limits. Numbers are seeded by `plans:seedDefaults` (idempotent internalMutation) — never auto-applied on schema deploy because tier changes are product decisions.
- [ ] **D-020:** Workspaces are the top-level multi-tenancy primitive. `projects.workspaceId` introduced as optional FK (this commit), backfilled by `migrations/create_personal_workspaces:run`, then made required in a follow-up. Membership in `workspace_members` (owner/admin/member). Personal workspaces auto-created on first sign-in.
- [ ] **D-021:** Stripe webhook idempotency is enforced via `webhook_events` table, keyed by `stripe_event_id`. Mark-processed only on successful handler completion; Stripe retries on 5xx.
- [ ] **D-022:** Server-side quota check is exposed as `plans.assertWithinQuotaInternal` — public Convex query gated on `POLARIS_CONVEX_INTERNAL_KEY`. Pattern matches `convex/system.ts`. Never call `verifyAuth` from Inngest functions; pipe `userId` from event payload.

### Task 0.2 — Update Article §8 to "8 tools"

**Files:** `docs/CONSTITUTION.md`

- [ ] Find Article §8 ("The N Agent Tools") and update the tool count + add the `edit_file` row matching the implementation in `code-agent.ts`. Cross-reference D-017 + D-018.

### Task 0.3 — Update ROADMAP.md if it exists

- [ ] Run `find docs -name ROADMAP.md`. If present, mark Phase 1.1 (edit_file), Phase 2 (plans + Stripe), Phase 4 (workspaces additive) as DONE. Add Phase 1.2/1.3 (sandbox lifecycle + run_command) as IN PROGRESS.

### Task 0.4 — Reconcile `/pricing` numbers with `plans:seedDefaults`

**Files:** `src/app/(marketing)/pricing/page.tsx`, `convex/plans.ts`

- [ ] Decide source of truth: `convex/plans.ts:SEED_ROWS` is authoritative.
- [ ] Either (a) update `/pricing` page copy to match seed values exactly (e.g. "50K tokens / month — ~100 medium agent runs"), or (b) update `seedDefaults` numbers to match marketing copy. Pick (a) — engineering numbers stay precise, marketing copy interprets.
- [ ] Test: assertion in `tests/unit/quota/plans.test.ts` that the marketing tier names align with the seed row ids.

**Verify:** `pnpm tsc --noEmit && pnpm vitest run tests/unit/quota`. Commit: `chore(constitution): D-018..D-022 + Article §8 update + pricing/plans reconcile`.

---

## Phase 1 — Agent capability (sandbox lifecycle + run_command + streaming)

**This is the biggest UX delta. After Phase 1, Polaris stops "writing code blind" and becomes a true coding agent.**

### Task 1.1 — Replace MockSandboxProvider hardcode in agent-loop.ts

**Files:** `src/features/conversations/inngest/agent-loop.ts`

- [ ] Replace `import { MockSandboxProvider } from "@/lib/sandbox/mock-provider"` with `import { getSandboxProvider } from "@/lib/sandbox"`.
- [ ] Replace `const sandbox = new MockSandboxProvider()` with `const sandbox = getSandboxProvider()`.
- [ ] Confirm `src/lib/sandbox/index.ts` already returns `MockSandboxProvider` when `SANDBOX_PROVIDER=mock` or `E2B_API_KEY` unset, so dev/test environments are unchanged.
- [ ] Test: existing tests still pass.

**Commit:** `feat(agent): use sandbox provider singleton instead of Mock hardcode (D-018)`

### Task 1.2 — Per-project sandbox lifecycle helpers (Convex)

**Files:** `convex/sandboxes.ts` (NEW), `convex/schema.ts` (no change — `sandboxes` table already exists)

- [ ] Create `convex/sandboxes.ts` with these functions, all `internalKey`-gated like `convex/system.ts`:
  - `getByProject({ internalKey, projectId }): Doc<"sandboxes"> | null`
  - `setForProject({ internalKey, projectId, sandboxId, expiresAt })` — upsert; sets `alive=true`, `createdAt=now`, `lastAlive=now`, `needsResync=false`
  - `markDead({ internalKey, sandboxId })` — sets `alive=false`, `needsResync=true`
  - `touch({ internalKey, sandboxId })` — bumps `lastAlive=now`

- [ ] **Test:** `tests/unit/sandbox/lifecycle-mutations.test.ts` — vitest with mocked `ctx.db` covering: insert, upsert, markDead idempotent, touch bumps timestamp.

**Commit:** `feat(sandbox): per-project lifecycle queries/mutations (D-018)`

### Task 1.3 — Wire lifecycle into agent-loop.ts

**Files:** `src/features/conversations/inngest/agent-loop.ts`

- [ ] Before constructing `AgentRunner`, fetch sandbox via `convex.query(api.sandboxes.getByProject, ...)`.
- [ ] If absent or `alive=false` or `expiresAt < now`: call `sandbox.create("nextjs", { timeoutMs: 24*60*60*1000 })`, then `convex.mutation(api.sandboxes.setForProject, ...)`.
- [ ] If `needsResync=true`: trigger a sync step (defer impl to `executor` — for now, just log).
- [ ] Wrap the entire `runner.run(...)` in a try/catch for `SandboxDeadError`. On catch: `markDead`, create new sandbox, retry once. After 1 retry, escalate to `NonRetriableError`.
- [ ] Pass the resolved `sandboxId` to `AgentRunner` via the `sandboxId` field already on `AgentRunner` constructor.

- [ ] **Test:** `tests/unit/inngest/agent-loop-sandbox.test.ts` — mock `convex.query/mutation` + `sandbox.create`. Cover: cold start (no row), warm reuse, dead-sandbox reprovisioning.

**Commit:** `feat(agent): per-project sandbox lifecycle in agent-loop (D-018)`

### Task 1.4 — Extend messages schema for tool stream

**Files:** `convex/schema.ts`

- [ ] Add to the `toolCalls` array element validator a new optional field:
  ```ts
  stream: v.optional(v.array(v.object({
    kind: v.union(v.literal("stdout"), v.literal("stderr")),
    line: v.string(),
    at: v.number(),
  }))),
  ```
- [ ] Run `npx convex dev --once` to push schema.

**Commit:** `feat(schema): toolCalls.stream[] for run_command output (D-018)`

### Task 1.5 — `appendToolStream` + `setToolResult` Convex mutations

**Files:** `convex/system.ts`

- [ ] Add `appendToolStream({ internalKey, messageId, toolUseId, kind, line })` — finds the message, finds the toolCall by `id === toolUseId`, pushes the stream entry. Truncate at 4 KB total per call (bounded queue).
- [ ] **Test:** stream append, truncation, missing toolUseId is no-op (don't throw — agent retries are normal).

**Commit:** `feat(convex): appendToolStream mutation for live tool output`

### Task 1.6 — Re-add `run_command` to code-agent.ts registry

**Files:** `src/lib/agents/code-agent.ts`, `src/lib/agents/system-prompt.ts`

- [ ] Add the tool definition next to `edit_file`. Args: `{ command: string, cwd?: string, timeoutMs?: number }`. Description warns: "60s default timeout. NOT for `npm run dev` (already running). Use for npm install / npm test / npm run build."
- [ ] Add `case "run_command":` in the executeToolCall switch:
  - Validate against `forbidden-commands.ts` patterns first → return `{ error: "FORBIDDEN: ${reason}" }`
  - Look up `sandboxes.getByProject` for the project's sandboxId. If absent, `{ error: "SANDBOX_DEAD: no sandbox; ask user to retry" }`
  - Call `sandbox.exec(sandboxId, command, { cwd, timeoutMs: opts.timeoutMs ?? 60_000, onStdout: line => convex.mutation(api.system.appendToolStream, ...), onStderr: ... })`
  - Truncate `stdout` and `stderr` returns to last 4 KB
  - Return `{ stdout, stderr, exitCode, durationMs }`
- [ ] Update `system-prompt.ts` to re-add `run_command(command, cwd?)` to the tool list. Restore `SANDBOX_DEAD` and `COMMAND_TIMEOUT` rows in the "When Tools Fail" section.
- [ ] **Test:** `tests/unit/tools/run-command.test.ts` — forbidden command rejected, happy path, sandbox missing, timeout maps to error.

**Commit:** `feat(agent): wire run_command tool with streaming output (D-018)`

### Task 1.7 — `<ToolOutputStream />` UI component

**Files:** `src/components/ai-elements/tool-output-stream.tsx` (NEW), `src/components/ai-elements/message.tsx`

- [ ] Create the component per Praxiom §7.7: `border-l-2 border-primary/40 pl-3 py-2 mt-2`, monospace, `max-h-48 overflow-y-auto scrollbar-thin`. stderr lines render `text-warning`. Empty state: italic muted-foreground "(no output yet)".
- [ ] Props: `{ lines?: Array<{ kind: "stdout" | "stderr"; line: string; at: number }> }`.
- [ ] In `message.tsx` tool-call rendering: when `tc.name === "run_command"`, render `<ToolOutputStream lines={tc.stream} />` below the tool-call card.
- [ ] **Test:** RTL render test in `tests/unit/ui/tool-output-stream.test.tsx` — empty state, multi-line, stderr coloring class applied.

**Commit:** `feat(ui): ToolOutputStream component for run_command streaming`

### Task 1.8 — Migrate `agent-loop.ts` to be the active path

**Files:** `src/app/api/messages/route.ts`, `src/app/api/inngest/route.ts`, `src/features/conversations/inngest/process-message.ts`

- [ ] Currently `/api/messages` emits `"message/sent"` which `processMessage` consumes. The new `agent-loop.ts` listens on `"agent/run"`. Switch the API to emit `"agent/run"` with the new payload shape `{ messageId, conversationId, projectId, userId }`. Keep `processMessage` registered for now (Article XIX migration) but make `agent-loop` primary.
- [ ] Add a 7-day deprecation note in `process-message.ts` header.
- [ ] Test by running existing message flow end-to-end against mock sandbox.

**Commit:** `feat(agent): switch /api/messages to emit agent/run event (Article XIX)`

---

## Phase 2 — Billing UI + Stripe checkout/portal

### Task 2.1 — `/api/billing/checkout` route

**Files:** `src/app/api/billing/checkout/route.ts` (NEW), `src/lib/billing/stripe-client.ts` (NEW shared)

- [ ] Body: `{ tier: "pro" | "team" }`. Auth required (Clerk).
- [ ] Resolve user's `customers` row; create Stripe Customer if `stripeCustomerId` is null (call `stripe.customers.create({ email, metadata: { userId } })`); upsert customer row with the new ID.
- [ ] `stripe.checkout.sessions.create({ mode: "subscription", line_items: [{ price: <price_lookup>, quantity: 1 }], client_reference_id: userId, customer: stripeCustomerId, success_url, cancel_url })`.
- [ ] Return `{ url: session.url }`.
- [ ] **Test:** `tests/unit/billing/checkout.test.ts` — mocked Stripe SDK + Convex client; happy path; missing tier → 400; non-allowlisted tier → 400.

### Task 2.2 — `/api/billing/portal` route

**Files:** `src/app/api/billing/portal/route.ts` (NEW)

- [ ] Auth required. Read user's `stripeCustomerId`. If absent → 400 "no subscription".
- [ ] `stripe.billingPortal.sessions.create({ customer, return_url })`.
- [ ] Return 302 redirect to `session.url` (so a `<Link href="/api/billing/portal">` works).

### Task 2.3 — Wire `/pricing` CTAs to checkout

**Files:** `src/app/(marketing)/pricing/page.tsx`

- [ ] Replace `cta.href: "/sign-up"` with `cta.action: "/api/billing/checkout?tier=pro"` (or POST form). Use a `<form action method="POST">` so JS isn't required, OR a small client component.
- [ ] Free tier remains `/sign-up`.

### Task 2.4 — Stripe webhook unit tests

**Files:** `tests/unit/billing/webhook.test.ts` (NEW)

- [ ] Mock Stripe SDK `webhooks.constructEvent` and `convex.mutation/query`.
- [ ] Cover: invalid signature → 400; idempotent duplicate → 200 with `idempotent: true`; each of the 4 event types calls correct upsert args; userId resolution priority (client_reference_id → metadata → getByStripeCustomer); handler error → 500 + NOT marked processed.

**Commit per task. Aggregate:** `feat(billing): Stripe checkout + portal + webhook tests`

### Task 2.5 — Plan tier badge in IDE rail

**Files:** `src/features/projects/components/ide-rail.tsx`, `src/features/billing/hooks/use-customer.ts` (NEW or reuse)

- [ ] Below `<UserButton />` in the rail, render a small badge: "FREE" / "PRO" / "TEAM" with Praxiom badge tones (`bg-surface-4 text-muted-foreground` / `bg-primary/15 text-primary` / `bg-success/15 text-success`).
- [ ] Click → `/settings#billing`.

### Task 2.6 — Usage meter in Settings → Billing

**Files:** `src/features/settings/components/settings-view.tsx`

- [ ] Add a new `UsageMeter` block inside `<BillingPane />`: queries `usage.getCurrentMonth` (or aggregate via existing usage table), renders `<progress>` bars for tokens / projects / deploys against the plan caps.
- [ ] At >=80% → warning tone; >=100% → destructive tone.
- [ ] Add a Convex query `convex/usage.ts:getCurrentMonth` (auth-bound) if it doesn't exist.

### Task 2.7 — `<QuotaBlockedToast />` + frontend handling

**Files:** `src/components/quota-blocked-toast.tsx` (NEW), `src/features/conversations/components/conversation-sidebar.tsx`

- [ ] When a `ky.post("/api/messages", ...)` returns 429 with `error: "quota_exceeded"`, parse the JSON and show `toast.custom(...)` with reason / current / limit + "Upgrade to Pro" link to `/pricing`.
- [ ] Same handling for any other 429 the app surfaces.

**Commit:** `feat(billing): plan tier badge + usage meter + quota toast`

---

## Phase 3 — Testing

### Task 3.1 — `assertWithinQuota` handler-level unit tests

**Files:** `tests/unit/quota/assert-within-quota.test.ts` (NEW)

- [ ] Mock `ctx.db.query/withIndex/first/collect`. Cover all 6 cases in the original audit:
  - free under tokens → ok
  - free at tokens → not ok
  - free over projects → not ok
  - pro under tokens → ok
  - pro over tokens → not ok
  - plan row missing → throws

### Task 3.2 — Workspaces unit tests

**Files:** `tests/unit/workspaces/workspaces.test.ts` (NEW)

- [ ] `create`: happy path inserts both rows, slug uniqueness with collisions appends `-2`, blank name throws.
- [ ] `getCurrent`: returns owned first, falls back to membership, returns null if neither.
- [ ] `listForUser`: returns all memberships with role.
- [ ] `invite`: rejects non-owner caller, idempotent on duplicate (workspaceId, userId).
- [ ] `updateRole`: blocks last-owner demotion.
- [ ] `removeMember`: blocks last-owner removal.

### Task 3.3 — Stripe webhook unit tests (already in 2.4)

### Task 3.4 — Sandbox lifecycle unit tests (already in 1.2 + 1.3)

### Task 3.5 — Consistency model tests

**Files:** `tests/unit/agents/consistency.test.ts` (NEW)

- [ ] Test: agent loop receives `attempt > 0` → loads checkpoint from `agent_checkpoints` and resumes from the saved iteration count.
- [ ] Test: `SandboxDeadError` mid-loop → marks sandbox dead, requests new sandbox, retries from checkpoint with `needsResync=true`.

### Task 3.6 — Clerk testing-token helper

**Files:** `tests/e2e/_helpers/clerk-auth.ts` (NEW)

- [ ] Helper that uses Clerk's testing token API (`@clerk/testing` package): given `E2E_CLERK_TEST_USER_ID` env, returns a JWT that the Playwright test can attach as a session cookie or Authorization header.
- [ ] Document the dashboard step: "Settings → Sessions → Create testing token" → value into `E2E_CLERK_TESTING_API_KEY`.

### Task 3.7 — Un-`fixme` E2E specs as preconditions land

**Files:** `tests/e2e/prompt-to-preview.spec.ts`, `chat-modify.spec.ts`, `github-import.spec.ts`, `deploy.spec.ts`, `quota-blocks-free-user.spec.ts`

- [ ] After 3.6 (Clerk helper) lands: replace `test.fixme(` with `test(` in `prompt-to-preview` and `quota-blocks-free-user`.
- [ ] After 1.2 (E2B lifecycle) lands AND a test E2B account is provisioned: un-fixme `chat-modify`.
- [ ] After a test public GitHub repo is published (e.g. `polarislabs/hello-next`): un-fixme `github-import`.
- [ ] After test Vercel + Supabase tokens are provisioned: un-fixme `deploy`.

### Task 3.8 — Rate-limiting integration test

**Files:** `tests/unit/rate-limit/route-integration.test.ts` (NEW)

- [ ] After Phase 6 (rate limiting wiring), ensure each protected route applies the limiter. Mock the Upstash client; assert `consume` is called with the correct (userId, route, limit) tuple.

**Commit per task; aggregate label:** `test: close §16 testing gaps`

---

## Phase 4 — Workspaces UI completion + scoping

### Task 4.1 — Email-based member display + invite UI

**Files:** `src/features/settings/components/settings-view.tsx`, `convex/clerk_users.ts` (NEW or reuse)

- [ ] Replace raw `userId` display with email + avatar. Either:
  - Add a Convex action that calls Clerk's Backend API `users.getUser(userId)` for each member, OR
  - Cache (userId → email) in a new lightweight `clerk_user_cache` table populated by the existing Clerk webhook.
- [ ] Build invite dialog: input email → POST `/api/workspaces/invite` (NEW) → server resolves email → Clerk userId via Backend API → `workspaces.invite({ workspaceId, userId, role })`.
- [ ] If email isn't a Clerk user yet: send Clerk Invitation via Backend API (`invitations.createInvitation`) AND pre-create the membership row.

### Task 4.2 — Auto-create personal workspace on first sign-in

**Files:** `src/app/api/webhooks/clerk/route.ts` (existing — extend)

- [ ] On `user.created` event: call `convex.mutation(api.workspaces.createPersonal, { userId, email })` (NEW internal mutation).
- [ ] `createPersonal` matches `migrations/create_personal_workspaces:run` shape but for a single user.

### Task 4.3 — Make `projects.workspaceId` required

**Files:** `convex/schema.ts`, `convex/projects.ts`

- [ ] Run migration first. Verify zero projects have `workspaceId == undefined`.
- [ ] Drop `v.optional(...)` wrapper from `projects.workspaceId`.
- [ ] **Test:** `pnpm tsc --noEmit && npx convex dev --once`.

### Task 4.4 — Filter projects by workspaceId

**Files:** `convex/projects.ts`

- [ ] `get`, `getPartial`, `getById` all gain a `workspaceId` filter (default = current workspace via Clerk identity → `workspaces.getCurrent`).
- [ ] `create` requires `workspaceId` arg; validate caller is a member.
- [ ] **Test:** new tests in `tests/unit/projects/scoping.test.ts` — caller in workspace A cannot read workspace B's projects.

### Task 4.5 — Workspace switcher actually scopes UI

**Files:** `src/features/workspaces/components/workspace-switcher.tsx`, `src/features/projects/hooks/use-projects.ts`

- [ ] Currently the switcher dropdown lists workspaces but clicking one is a no-op. Add a hook `useSetActiveWorkspace` that writes the choice to a cookie / localStorage, then re-renders project list. Or: route param (`/workspaces/[slug]/projects/...`).
- [ ] Use cookie approach for simplicity — `polaris_active_workspace` cookie, read in middleware → set on the Convex client per-request.

**Commit:** `feat(workspaces): UI completion (invite + auto-create + scoping)`

---

## Phase 5 — Vercel AI SDK final removal

### Task 5.1 — Inline AI SDK types into 6 files

**Files:**
- `src/components/ai-elements/message.tsx` (FileUIPart, UIMessage)
- `src/components/ai-elements/confirmation.tsx` (ToolUIPart)
- `src/components/ai-elements/context.tsx` (LanguageModelUsage)
- `src/components/ai-elements/prompt-input.tsx` (ChatStatus, FileUIPart)
- `src/components/ai-elements/tool.tsx` (ToolUIPart)
- `src/components/ai-elements/image.tsx` (Experimental_GeneratedImage)

- [ ] Create `src/components/ai-elements/types.ts` with minimal local definitions:
  ```ts
  export type UIMessageRole = "system" | "user" | "assistant" | "data" | "tool";
  export interface UIMessage { id: string; role: UIMessageRole; content: string; }
  export interface FileUIPart { type: "file"; data: string; mimeType?: string; }
  export type ToolUIPart =
    | { type: "tool-call"; toolCallId: string; toolName: string; args: unknown }
    | { type: "tool-result"; toolCallId: string; toolName: string; result: unknown };
  export type ChatStatus = "submitted" | "streaming" | "ready" | "error";
  export interface LanguageModelUsage { promptTokens?: number; completionTokens?: number; totalTokens?: number; }
  export interface Experimental_GeneratedImage { base64: string; mimeType: string; }
  ```
- [ ] Replace `import type { X } from "ai"` with `import type { X } from "./types"` in all 6 files.

### Task 5.2 — Drop SDK packages

- [ ] `git grep "@ai-sdk\|from \"ai\"" src/ convex/ tests/ docs/` — must return empty.
- [ ] `pnpm remove @ai-sdk/anthropic @ai-sdk/google ai`
- [ ] `pnpm tsc --noEmit && pnpm test && pnpm build` — all green.

**Commit:** `refactor: complete Vercel AI SDK removal (D-007)`

---

## Phase 6 — Rate limiting + per-tier limits

### Task 6.1 — Wire upstash limiter to `/api/messages`, `/api/suggestion`, `/api/quick-edit`

**Files:** `src/app/api/messages/route.ts`, `src/app/api/suggestion/route.ts`, `src/app/api/quick-edit/route.ts`, `src/lib/rate-limit/middleware.ts` (NEW)

- [ ] Create a thin middleware helper `applyRateLimit(req, { userId, route, limit })`. Internally calls `upstashLimiter.consume`.
- [ ] On rejection: return 429 with `Retry-After` header.

### Task 6.2 — Per-tier limits

**Files:** `src/lib/rate-limit/per-tier.ts` (NEW)

- [ ] Read user plan via Convex; map plan → bucket size:
  - free: 10 req/min for messages, 20/min for suggestion, 5/min for quick-edit
  - pro: 60 req/min for messages, 120/min for suggestion, 30/min for quick-edit
  - team: 240 req/min for messages, 480/min for suggestion, 120/min for quick-edit
- [ ] **Test:** `tests/unit/rate-limit/per-tier.test.ts`.

**Commit:** `feat(rate-limit): per-tier limits on messages/suggestion/quick-edit`

---

## Phase 7 — Performance instrumentation + Sentry

### Task 7.1 — Sentry performance markers

**Files:** `src/lib/observability/spans.ts` (NEW), call sites in `process-message.ts`, `agent-loop.ts`, `scaffold/route.ts`, `deploy-pipeline.ts`

- [ ] Wrap each major step in `Sentry.startSpan({ op, name }, async () => { ... })`. Specifically:
  - `agent.iteration` (each loop iteration — assert <3s P50, <8s P95)
  - `prompt.to.preview` (from `/api/messages` → first preview URL — <60s P50, <120s P95)
  - `sandbox.boot` (sandbox provisioning — <8s P50, <15s P95)
  - `tool.run_command` (each shell command — <60s)

### Task 7.2 — Sentry alert rules (config-as-code)

**Files:** `docs/runbooks/sentry-alerts.md` (NEW) — describes the dashboard config since alerts live in the Sentry product UI

- [ ] Document the rules to create:
  - P95 of `prompt.to.preview` > 120s for 5 min → page `#oncall`
  - P95 of `agent.iteration` > 8s for 10 min → page
  - Error rate on `/api/messages` > 1% for 5 min → page
  - Stripe webhook handler error rate > 0% over 1 hr → page
- [ ] (No code commit — runbook guidance for the operator.)

### Task 7.3 — `/api/health` budget

**Files:** `src/app/api/health/route.ts`

- [ ] Add a budget assertion: total handler should respond in <500ms. Tag the trace.

**Commit:** `feat(observability): performance spans + alert runbook`

---

## Phase 8 — Praxiom integration stub

### Task 8.1 — `/api/praxiom/import` 501 stub

**Files:** `src/app/api/praxiom/import/route.ts` (NEW)

- [ ] Per CONSTITUTION §18.5 — endpoint exists but returns 501 with body `{ error: "praxiom_integration_pending", trackingIssue: "POL-18" }`.
- [ ] **Test:** `tests/e2e/praxiom-stub.spec.ts` — POST returns 501 + JSON shape.

**Commit:** `feat(praxiom): /api/praxiom/import 501 stub (Article §18)`

---

## Phase 9 — Optimistic mutations + small TODOs

### Task 9.1 — `use-files.ts` optimistic mutations

**Files:** `src/features/projects/hooks/use-files.ts`

- [ ] Add `withOptimisticUpdate` to each of the 4 mutations (createFile, createFolder, deleteFile, renameFile). Match Convex's standard pattern.
- [ ] **Test:** RTL test: file appears in tree before server confirms.

### Task 9.2 — `/api/messages` "Check for processing messages"

**Files:** `src/app/api/messages/route.ts`

- [ ] Before creating a new user message, check if there's already a `status: "processing"` message in the conversation. If yes, return 409 `{ error: "in_flight" }` (the client should call `/api/messages/cancel` first).

### Task 9.3 — Header comment "TODO follow-up" in schema for personal workspace upsert

**Files:** `convex/schema.ts:385`

- [ ] Remove the TODO once Phase 4.2 (auto-create on Clerk webhook) lands. Comment is now a description of the implemented behaviour, not a TODO.

**Commit:** `chore: optimistic file mutations + processing-message guard + comment cleanup`

---

## Phase 10 — Documentation

### Task 10.1 — README.md

- [ ] One-page overview: what Polaris is, prerequisites, the 4-step setup (`pnpm i` → `cp .env.example .env.local` → `pnpm convex:dev` → `pnpm dev`), then the 3 idempotent post-pull commands (`seedDefaults` / `migrations/create_personal_workspaces` / Stripe env).

### Task 10.2 — docs/ARCHITECTURE.md

- [ ] Diagram: Browser ↔ Next.js API ↔ Convex / Inngest ↔ Sandbox / Stripe / Clerk. Plus the agent loop sequence diagram with all 4 error layers.

### Task 10.3 — docs/RUNBOOK.md

- [ ] Common operational scenarios + their fixes:
  - "Convex function not found" → `pnpm convex:dev`
  - "Sandbox dead loop" → check E2B status / fall back to mock
  - "Stripe webhook 500s" → check `STRIPE_WEBHOOK_SECRET`
  - "Quota exceeded for free user" → /pricing or `seedDefaults`
  - "Workspace migration not run" → `npx convex run migrations/create_personal_workspaces:run`

### Task 10.4 — Refresh docs/ROADMAP.md

- [ ] Move completed Phase 1-4 items to "Done", current state of remaining phases.

**Commit:** `docs: README + ARCHITECTURE + RUNBOOK + ROADMAP refresh`

---

## Required user-side actions across all phases

| When | Command | Reason |
|---|---|---|
| After every schema change | `pnpm convex:dev` | Push to deployed Convex |
| After Phase 0 commit | (none) | Constitution edits are doc-only |
| After Phase 2.1–2.2 | Add `STRIPE_WEBHOOK_SECRET` and `STRIPE_SECRET_KEY` to `.env.local` + Vercel env | Webhook verification |
| After Phase 2 | Configure Stripe products with `lookup_key="polaris_pro"` and `polaris_team` | Plan resolution |
| After Phase 4.2 | Add Clerk webhook URL `/api/webhooks/clerk` with `user.created` event | Auto-workspace |
| After Phase 4.3 (workspaceId required) | Run `npx convex run migrations/create_personal_workspaces:run` again to verify zero unscoped projects | Migration completeness |
| After Phase 6 | Add `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` if not already set | Rate limiter backend |
| After Phase 7 | Configure Sentry alert rules per `docs/runbooks/sentry-alerts.md` | Production paging |

---

## Self-review checklist (run at end of every phase)

1. `pnpm tsc --noEmit` — zero errors
2. `pnpm lint` — zero NEW errors in modified files (pre-existing test-file lint debt is OK)
3. `pnpm vitest run tests/unit` — all green
4. `pnpm exec playwright test --list` — all expected specs registered (some `fixme`)
5. `npx convex dev --once` — schema deploys cleanly
6. `git diff main` — no unrelated drift
7. CONSTITUTION decision log updated if architectural

---

## Effort estimate (calendar)

Each phase is 1–3 focused subagent sessions. Cumulative:

| Phase | Hours | Subagent sessions |
|---|---|---|
| 0 — Constitution | 2 | 1 |
| 1 — Agent capability | 12 | 4 |
| 2 — Billing UI | 8 | 3 |
| 3 — Testing | 14 | 4 |
| 4 — Workspaces UI | 10 | 3 |
| 5 — AI SDK removal | 3 | 1 |
| 6 — Rate limiting | 4 | 1 |
| 7 — Sentry / perf | 6 | 2 |
| 8 — Praxiom stub | 1 | 1 |
| 9 — TODOs | 3 | 1 |
| 10 — Docs | 4 | 1 |
| **Total** | **67** | **22** |

Phases can run partly in parallel (Phase 5/6/8 are independent of Phase 1). Realistic calendar with focused execution and review: 2 weeks.

---

## Decision Log additions (proposed)

Already covered in Task 0.1, restated here for traceability:

- **D-018**: Sandbox lifecycle is per-project, persisted, reprovisioned on death; provider via `getSandboxProvider()` env-driven.
- **D-019**: `plans` table is source-of-truth; seeded by idempotent `internalMutation`, never auto-applied on schema deploy.
- **D-020**: Workspaces are top-level; `projects.workspaceId` optional → required after migration. Auto-created on first sign-in.
- **D-021**: Stripe webhook idempotency via `webhook_events`; mark-processed only on success.
- **D-022**: `assertWithinQuotaInternal` is public Convex query gated on `internalKey` for HTTP / Inngest callers.

---

## What "100% complete" means

After all phases land:
- Every TODO/FIXME in `src/` and `convex/` is either resolved or replaced with a Decision-Log-cited "this is by design" note.
- Every CONSTITUTION article is ✅ in the audit table — no ⚠️.
- `pnpm test` passes cleanly. `pnpm exec playwright test` runs all 5 mandatory E2E specs (un-fixme'd) on the staging URL.
- `package.json` has zero references to `@ai-sdk/*` or `"ai"`.
- The agent can run `npm install`, see TypeScript errors from `npm run build`, and iterate — stop being a code generator, become a coding agent.
- Every API route has rate limiting; every quota-able operation has a pre-check; every Stripe lifecycle event is handled idempotently.
- Workspaces are first-class — every project belongs to one; members are invited by email; the rail switcher actually scopes the UI.
- Sentry has live performance dashboards + alerts; the SLOs in §14 are observed, not just documented.

That is the bar. This plan is the ledger to clear to reach it.
